{
  "name": "log2trace-viewer",
  "version": "1.0.0",
  "description": "An interactive visualization library for OpenTelemetry trace data",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "minify-css": "lightningcss --minify src/styles.css -o src/styles.min.css",
    "build": "npm run minify-css && tsc && esbuild src/index.ts --bundle --minify --sourcemap --metafile=dist/meta.json --format=esm --outfile=dist/index.js --loader:.css=text",
    "check": "tsc --noEmit",
    "demo:kill": "npx kill-port 3000",
    "demo": "npm run demo:kill && npm run build && node demo/server.js"
  },
  "keywords": [
    "opentelemetry",
    "trace",
    "visualization",
    "observability",
    "distributed-tracing",
    "waterfall",
    "timeline"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.27.3",
    "lightningcss-cli": "^1.31.1",
    "typescript": "^5.9.3"
  },
  "files": [
    "dist",
    "src"
  ]
}
