# log2trace-ui

Zero-dependency Web Component that renders OpenTelemetry traces as an interactive waterfall timeline, with built-in log-to-trace transformation.

Drop `<trace-visualizer>` into any HTML page or framework.